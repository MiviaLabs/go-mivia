openapi: 3.1.0
info:
  title: MiviaLabs Agent Control API
  version: 0.1.0
  description: Localhost-only bootstrap REST contract for task, research-run, project context, reliability, and redacted agent-run metadata.
servers:
  - url: http://127.0.0.1:8080/api/v1
paths:
  /tasks:
    post:
      operationId: createTask
      summary: Create task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '201':
          description: Created task
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
        '400':
          $ref: '#/components/responses/BadRequest'
  /tasks/{id}:
    get:
      operationId: getTask
      summary: Get task
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Task
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
        '404':
          $ref: '#/components/responses/NotFound'
  /research-runs:
    post:
      operationId: createResearchRun
      summary: Create research run metadata
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateResearchRunRequest'
      responses:
        '201':
          description: Created research run
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResearchRun'
        '400':
          $ref: '#/components/responses/BadRequest'
  /research-runs/{id}:
    get:
      operationId: getResearchRun
      summary: Get research run metadata
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Research run
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResearchRun'
        '404':
          $ref: '#/components/responses/NotFound'
  /research-runs/{id}/sources:
    post:
      operationId: createResearchSource
      summary: Create redacted research source metadata
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateResearchSourceRequest'
      responses:
        '201':
          description: Created research source metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResearchSource'
        '400':
          $ref: '#/components/responses/BadRequest'
  /research-runs/{id}/sources/{source_id}:
    get:
      operationId: getResearchSource
      summary: Get redacted research source metadata
      parameters:
        - $ref: '#/components/parameters/ID'
        - name: source_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        '200':
          description: Research source metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResearchSource'
        '404':
          $ref: '#/components/responses/NotFound'
  /agent-runs:
    post:
      operationId: createAgentRun
      summary: Create redacted agent run metadata
      description: Stores metadata only. Raw prompts, completions, source dumps, raw stderr, secrets, credentials, provider payloads, absolute roots, and PII are prohibited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAgentRunRequest'
      responses:
        '201':
          description: Created redacted agent run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRun'
        '400':
          $ref: '#/components/responses/BadRequest'
  /agent-runs/{id}:
    get:
      operationId: getAgentRun
      summary: Get redacted agent run metadata
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Redacted agent run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRun'
        '404':
          $ref: '#/components/responses/NotFound'
  /agent-runs/{id}/steps:
    post:
      operationId: appendAgentRunStep
      summary: Append redacted agent run step metadata
      description: Appends one metadata-only step. Raw tool payloads, prompts, completions, source dumps, raw stderr, secrets, credentials, absolute roots, and PII are prohibited.
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppendAgentStepRequest'
      responses:
        '200':
          description: Updated redacted agent run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /agent-runs/{id}/promotions:
    post:
      operationId: promoteAgentArtifact
      summary: Record agent artifact promotion-gate decision
      description: Records metadata-only promotion vocabulary for an existing agent-run artifact. Stores refs, state, verifier ref, and bounded decision text only; raw prompts, source dumps, raw stderr, roots, secrets, provider payloads, and PII are prohibited.
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromoteAgentArtifactRequest'
      responses:
        '200':
          description: Updated redacted agent run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /agent-runs/{id}/complete:
    post:
      operationId: completeAgentRun
      summary: Complete redacted agent run metadata
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteAgentRunRequest'
      responses:
        '200':
          description: Completed redacted agent run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects:
    get:
      operationId: listProjects
      summary: List configured local projects
      responses:
        '200':
          description: Configured local project metadata without root paths
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectList'
  /projects/{id}:
    get:
      operationId: getProject
      summary: Get configured local project metadata
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Project metadata without root path exposure
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/digest-runs:
    post:
      operationId: createProjectDigestRun
      summary: Run a manual metadata-only project digest
      description: Stores project/file metadata and metadata fingerprints only; raw source content and file-content hashes are not stored or returned.
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '201':
          description: Created metadata-only project digest run
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectDigestRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/context-health:
    get:
      operationId: getProjectContextHealth
      summary: Get project context health
      description: Returns deterministic project context readiness and freshness metadata without roots, source text, skipped sensitive content, raw errors, prompts, provider payloads, secrets, or PII.
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Project context health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContextHealth'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/impact/analyze:
    post:
      operationId: analyzeProjectImpact
      summary: Analyze changed-path impact
      description: Deterministic local impact analysis from changed paths or governed workspace diff metadata, including graph-backed references, callers, and implementer edges. It does not return raw diff content.
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImpactAnalysisRequest'
      responses:
        '200':
          description: Impact analysis
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImpactAnalysis'
        '400':
          $ref: '#/components/responses/BadRequest'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /projects/{id}/context-pack:
    post:
      operationId: buildProjectContextPack
      summary: Build bounded project context pack
      description: Composes bounded text search hits, file metadata, symbol metadata, and optional impact metadata from existing local indexed context. It creates no new storage and returns no roots, raw diffs, external provider data, or uncapped chunk source.
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContextPackBuildRequest'
      responses:
        '200':
          description: Bounded project context pack
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContextPack'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /projects/{id}/claims/check:
    post:
      operationId: checkProjectClaims
      summary: Check stale documentation claims
      description: Deterministic claim check for selected stable docs/contracts and registered REST/MCP names. It does not use LLM judgment or broad crawling.
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClaimCheckRequest'
      responses:
        '200':
          description: Claim check result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClaimCheckResult'
        '400':
          $ref: '#/components/responses/BadRequest'
  /projects/{id}/workspace/git/status:
    get:
      operationId: getProjectWorkspaceGitStatus
      summary: Get governed project git status
      description: Parsed status for globally enabled and per-project opted-in content_graph workspaces. Does not expose roots, raw command lines, or raw stderr.
      parameters:
        - $ref: '#/components/parameters/ID'
        - name: include_untracked
          in: query
          schema:
            type: boolean
            default: true
        - name: path_prefix
          in: query
          schema:
            type: string
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
      responses:
        '200':
          description: Governed git status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceGitStatus'
        '400':
          $ref: '#/components/responses/BadRequest'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /projects/{id}/workspace/git/diff:
    get:
      operationId: getProjectWorkspaceGitDiff
      summary: Get governed project git diff
      description: Capped safe diff for opted-in content_graph workspaces. No arbitrary shell, commit, push, checkout, reset, raw patch, roots, command lines, or raw stderr are exposed.
      parameters:
        - $ref: '#/components/parameters/ID'
        - name: scope
          in: query
          schema:
            type: string
            enum: [working_tree, staged, head]
            default: working_tree
        - name: file_id
          in: query
          schema:
            type: string
        - name: relative_path
          in: query
          schema:
            type: string
        - name: path_prefix
          in: query
          schema:
            type: string
        - name: context_lines
          in: query
          schema:
            type: integer
            minimum: 0
            maximum: 10
        - name: max_diff_bytes
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 262144
        - $ref: '#/components/parameters/PageToken'
      responses:
        '200':
          description: Governed capped diff
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceGitDiff'
        '400':
          $ref: '#/components/responses/BadRequest'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /projects/{id}/workspace/files/read:
    get:
      operationId: readProjectWorkspaceFile
      summary: Read one current workspace file
      description: Reads one eligible current text file by file_id or relative_path and returns an opaque edit token. Tokens are not content hashes.
      parameters:
        - $ref: '#/components/parameters/ID'
        - name: file_id
          in: query
          schema:
            type: string
        - name: relative_path
          in: query
          schema:
            type: string
        - name: max_bytes
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 262144
      responses:
        '200':
          description: Current eligible file text
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceFile'
        '400':
          $ref: '#/components/responses/BadRequest'
  /projects/{id}/workspace/files/edit:
    post:
      operationId: editProjectWorkspaceFile
      summary: Apply token-guarded exact byte-span file edits
      description: Applies ordered non-overlapping exact edits only after a current edit token and old_text spans match. Successful non-dry-run edits queue path ingestion.
      parameters:
        - $ref: '#/components/parameters/ID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkspaceFileEditRequest'
      responses:
        '200':
          description: Edit result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceFileEditResult'
        '400':
          $ref: '#/components/responses/BadRequest'
  /projects/{id}/ingestion-runs:
    post:
      operationId: createProjectIngestionRun
      summary: Submit manual content graph ingestion
      description: Queues bounded localhost-only ingestion for an opted-in content_graph project and returns run metadata without waiting for scan completion. Raw DB queries, absolute roots, skipped sensitive content, secrets, PII, raw prompts, and provider payloads are not returned.
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '201':
          description: Created ingestion run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIngestionRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/search-index/rebuild:
    post:
      operationId: rebuildProjectSearchIndex
      summary: Rebuild the local project search index
      description: Queues repair of the local SQLite FTS search index for an opted-in content_graph project by deleting indexed search rows and re-running governed ingestion. Returns queued run metadata without waiting for repair completion; poll the ingestion run status before relying on search results. It does not expose raw database queries, roots, content hashes, skipped sensitive content, raw prompts, provider payloads, or raw SQLite/FTS errors.
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '202':
          description: Queued repair ingestion run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIngestionRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/ingestion-runs/latest:
    get:
      operationId: getLatestProjectIngestionRun
      summary: Get latest content graph ingestion run metadata
      description: Returns safe run metadata only. It does not include roots, raw paths, source snippets, sensitive matches, or raw errors.
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Latest ingestion run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIngestionRun'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/ingestion-runs/{run_id}:
    get:
      operationId: getProjectIngestionRun
      summary: Get manual content graph ingestion run metadata
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/RunID'
      responses:
        '200':
          description: Ingestion run metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIngestionRun'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/files:
    get:
      operationId: listProjectFiles
      summary: List project ingestion file metadata
      description: Returns bounded file metadata only. Sensitive skips omit relative paths and content hashes. Optional extension filtering is case-insensitive and normalizes to a leading dot.
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: status
          in: query
          schema:
            type: string
            enum: [eligible, skipped, absent]
        - name: extension
          in: query
          description: Optional file extension filter, with or without a leading dot. Path separators and whitespace are invalid.
          schema:
            type: string
            minLength: 1
      responses:
        '200':
          description: Project files
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFileList'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/files/{file_id}:
    get:
      operationId: getProjectFile
      summary: Get project ingestion file metadata
      description: Returns bounded metadata for one opaque file id. Sensitive skips omit relative paths, extensions, and content hashes.
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/FileID'
      responses:
        '200':
          description: Project file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/files/{file_id}/chunks:
    get:
      operationId: listProjectFileChunks
      summary: List bounded project file chunks
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/FileID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: max_chunk_bytes
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Project file chunks
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectChunkList'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/files/{file_id}/outline:
    get:
      operationId: getProjectFileOutline
      summary: Get project file outline
      description: Returns file, heading, symbol, and chunk location metadata. Symbol filters can bound large outlines. Chunk text is omitted by default, but eligible stored chunk text can be requested with include_chunk_text and max_chunk_bytes.
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/FileID'
        - name: kind
          in: query
          schema:
            type: string
        - name: name_prefix
          in: query
          schema:
            type: string
        - name: symbol_page_size
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: symbol_page_token
          in: query
          schema:
            type: string
        - name: include_chunk_text
          in: query
          schema:
            type: boolean
        - name: max_chunk_bytes
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Project file outline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFileOutline'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/symbols:
    get:
      operationId: listProjectSymbols
      summary: List project symbol metadata
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: kind
          in: query
          schema:
            type: string
        - name: name_prefix
          in: query
          schema:
            type: string
        - name: file_id
          in: query
          schema:
            type: string
        - name: extension
          in: query
          schema:
            type: string
        - name: package
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Project symbols
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolList'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/search/text:
    get:
      operationId: searchProjectText
      summary: Search eligible indexed project text
      description: Literal-only bounded search over eligible ContentChunk nodes. Snippets are capped. Skipped, denied, sensitive, absent, unindexed files, roots, content hashes, and raw datastore errors are not returned. Results may be stale until ingestion catches up.
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: query
          in: query
          required: true
          schema: {type: string, minLength: 1, maxLength: 256}
        - name: mode
          in: query
          schema: {type: string, enum: [literal]}
        - name: case_sensitive
          in: query
          schema: {type: boolean}
        - name: extension
          in: query
          schema: {type: string}
        - name: path_prefix
          in: query
          schema: {type: string}
        - name: max_snippet_bytes
          in: query
          schema: {type: integer, minimum: 1, maximum: 1024}
        - name: max_matches
          in: query
          schema: {type: integer, minimum: 1, maximum: 100}
      responses:
        '200':
          description: Project text search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTextSearchResultList'
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
  /projects/{id}/search/files:
    get:
      operationId: searchProjectFiles
      summary: Search eligible indexed project files
      description: Searches safe project-relative eligible file metadata by path substring. Skipped, denied, sensitive, and absent files are excluded.
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: extension
          in: query
          schema: {type: string}
        - name: path_prefix
          in: query
          schema: {type: string}
        - name: path_contains
          in: query
          schema: {type: string}
        - name: case_sensitive
          in: query
          schema: {type: boolean}
      responses:
        '200':
          description: Project file search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFileList'
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
  /projects/{id}/search/symbols:
    get:
      operationId: searchProjectSymbols
      summary: Search eligible indexed project symbols
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: kind
          in: query
          schema: {type: string}
        - name: name_prefix
          in: query
          schema: {type: string}
        - name: name_contains
          in: query
          schema: {type: string}
        - name: file_id
          in: query
          schema: {type: string}
        - name: extension
          in: query
          schema: {type: string}
        - name: package
          in: query
          schema: {type: string}
        - name: receiver
          in: query
          schema: {type: string}
        - name: case_sensitive
          in: query
          schema: {type: boolean}
      responses:
        '200':
          description: Project symbol search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolList'
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
  /projects/{id}/search/references:
    get:
      operationId: searchProjectReferences
      summary: Search eligible indexed project references
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: name_contains
          in: query
          schema: {type: string}
        - name: target_name_contains
          in: query
          schema: {type: string}
        - name: enclosing_contains
          in: query
          schema: {type: string}
        - name: extension
          in: query
          schema: {type: string}
        - name: path_prefix
          in: query
          schema: {type: string}
        - name: resolution_status
          in: query
          schema: {type: string}
        - name: confidence
          in: query
          schema: {type: string}
        - name: case_sensitive
          in: query
          schema: {type: boolean}
      responses:
        '200':
          description: Project reference search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolReferenceList'
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
  /projects/{id}/search/calls:
    get:
      operationId: searchProjectCalls
      summary: Search eligible indexed project calls
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: name_contains
          in: query
          schema: {type: string}
        - name: caller_name_contains
          in: query
          schema: {type: string}
        - name: callee_name_contains
          in: query
          schema: {type: string}
        - name: extension
          in: query
          schema: {type: string}
        - name: path_prefix
          in: query
          schema: {type: string}
        - name: resolution_status
          in: query
          schema: {type: string}
        - name: confidence
          in: query
          schema: {type: string}
        - name: case_sensitive
          in: query
          schema: {type: boolean}
      responses:
        '200':
          description: Project call search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolCallEdgeList'
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
  /projects/{id}/search/ast/queries:
    get:
      operationId: listProjectASTQueries
      summary: List eligible indexed project AST query catalog
      description: Returns supported named AST query IDs, languages, capture names, query versions, matching extensions, and safe per-language coverage counts scoped to file_too_large. Raw Tree-sitter query text and raw Tree-sitter query syntax are not exposed. Sensitive, denied, absent, parse-error, and other skipped files are not catalog inputs. Oversized files are represented only as safe coverage gaps through file/ingestion metadata such as skipped_reason=file_too_large, size, and reason counts. Source text, chunks, snippets, content hashes, skipped sensitive text, roots, secrets, PII, raw prompts, provider payloads, and raw parser/SQLite/FTS/Tree-sitter errors are not returned.
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Project AST query catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectASTQueryCatalog'
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
  /projects/{id}/search/ast:
    get:
      operationId: searchProjectAST
      summary: Search eligible indexed project AST
      description: Runs a named Tree-sitter structural query over eligible indexed chunks only. The query parameter is a catalog id, not raw Tree-sitter syntax. Dart supports normal structural query ids plus Flutter-specific flutter_widgets and flutter_build_methods. Sensitive, denied, absent, parse-error, oversized, and other skipped files are unreachable from AST search. Oversized files may appear only as file_too_large coverage counts, file metadata with skipped_reason=file_too_large, and ingestion reason counts. Skipped sensitive content, roots, content hashes, raw parser internals, raw prompts, provider payloads, secrets, PII, and raw datastore errors are not returned.
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
        - name: language
          in: query
          required: true
          schema: {type: string, enum: [go, python, javascript, jsx, typescript, tsx, csharp, dart]}
        - name: query
          in: query
          required: true
          schema:
            type: string
            enum: [function_declarations, class_declarations, type_declarations, call_expressions, imports, test_functions, assignments, error_handling, flutter_widgets, flutter_build_methods]
        - name: captures
          in: query
          description: Optional comma-separated capture-name allowlist from the named query.
          schema: {type: string}
        - name: extension
          in: query
          schema: {type: string}
        - name: path_prefix
          in: query
          schema: {type: string}
        - name: max_matches
          in: query
          schema: {type: integer, minimum: 1, maximum: 100}
        - name: max_snippet_bytes
          in: query
          schema: {type: integer, minimum: 1, maximum: 1024}
      responses:
        '200':
          description: Project AST search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectASTSearchResultList'
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
  /projects/{id}/symbols/{symbol_id}/source:
    get:
      operationId: getProjectSymbolSource
      summary: Get bounded eligible source text for a project symbol
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/SymbolID'
        - name: max_source_bytes
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Bounded symbol source
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolSource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/symbols/{symbol_id}/references:
    get:
      operationId: listProjectSymbolReferences
      summary: List bounded references for a project symbol
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/SymbolID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
      responses:
        '200':
          description: Project symbol references
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolReferenceList'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/symbols/{symbol_id}/callers:
    get:
      operationId: listProjectSymbolCallers
      summary: List direct caller edges for a project symbol
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/SymbolID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
      responses:
        '200':
          description: Project symbol caller edges
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolCallEdgeList'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/symbols/{symbol_id}/callees:
    get:
      operationId: listProjectSymbolCallees
      summary: List direct callee edges for a project symbol
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/SymbolID'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/PageToken'
      responses:
        '200':
          description: Project symbol callee edges
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolCallEdgeList'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /projects/{id}/symbols/{symbol_id}/call-graph:
    get:
      operationId: getProjectSymbolCallGraph
      summary: Traverse bounded direct call edges for a project symbol
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/SymbolID'
        - name: direction
          in: query
          schema:
            type: string
            enum: [callers, callees, both]
        - name: max_depth
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 5
        - name: max_nodes
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: Project symbol call graph
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSymbolCallGraph'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
components:
  parameters:
    ID:
      name: id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    RunID:
      name: run_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    FileID:
      name: file_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    SymbolID:
      name: symbol_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
      description: Stable opaque file id; never an absolute path.
    PageSize:
      name: page_size
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
    PageToken:
      name: page_token
      in: query
      schema:
        type: string
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ServiceUnavailable:
      description: Runtime dependency unavailable
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
  schemas:
    CreateTaskRequest:
      type: object
      additionalProperties: false
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
    Task:
      type: object
      additionalProperties: false
      required: [id, title, status, created_at, updated_at]
      properties:
        id:
          type: string
        title:
          type: string
        status:
          type: string
          enum: [pending]
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateResearchRunRequest:
      type: object
      additionalProperties: false
      required: [task_id, goal_summary]
      properties:
        task_id:
          type: string
          minLength: 1
        goal_summary:
          type: string
          minLength: 1
          maxLength: 500
          description: Redacted goal summary only; raw prompts, source content, PII, and provider payloads are prohibited.
    ResearchRun:
      type: object
      additionalProperties: false
      required: [id, task_id, goal_summary, status, created_at, updated_at]
      properties:
        id:
          type: string
        task_id:
          type: string
        goal_summary:
          type: string
        status:
          type: string
          enum: [pending]
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateResearchSourceRequest:
      type: object
      additionalProperties: false
      required: [artifact_ref, source_type, summary]
      properties:
        artifact_ref:
          type: string
          minLength: 1
          description: URL or artifact reference; sensitive query values are redacted before storage.
        source_type:
          type: string
          minLength: 1
        summary:
          type: string
          minLength: 1
          description: Summary is redacted before storage; raw source content is not accepted.
        policy_metadata:
          type: object
          additionalProperties:
            type: string
    ResearchSource:
      type: object
      additionalProperties: false
      required: [id, research_run_id, artifact_ref, source_type, summary, content_hash, retrieved_at, policy_metadata]
      properties:
        id:
          type: string
        research_run_id:
          type: string
        artifact_ref:
          type: string
        source_type:
          type: string
        summary:
          type: string
        content_hash:
          type: string
        retrieved_at:
          type: string
          format: date-time
        policy_metadata:
          type: object
          additionalProperties:
            type: string
    CreateAgentRunRequest:
      type: object
      additionalProperties: false
      required: [project_id]
      properties:
        project_id:
          type: string
          minLength: 1
        task_id:
          type: string
        summary:
          type: string
          description: Redacted summary only; raw prompts, completions, source, stderr, secrets, provider payloads, and PII are prohibited.
        changed_files:
          type: array
          items:
            type: string
            description: Safe project-relative path only.
        verifiers:
          type: array
          items:
            $ref: '#/components/schemas/AgentVerifier'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/AgentArtifact'
    AppendAgentStepRequest:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        run_id:
          type: string
          description: Optional body alias; path id is authoritative for REST.
        tool_name:
          type: string
        tool_category:
          type: string
        status:
          type: string
          enum: [running, completed, failed]
        failure_category:
          type: string
        notes:
          type: string
        changed_files:
          type: array
          items:
            type: string
        verifiers:
          type: array
          items:
            $ref: '#/components/schemas/AgentVerifier'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/AgentArtifact'
    CompleteAgentRunRequest:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        run_id:
          type: string
          description: Optional body alias; path id is authoritative for REST.
        status:
          type: string
          enum: [completed, failed]
        failure_category:
          type: string
        summary:
          type: string
        changed_files:
          type: array
          items:
            type: string
        verifiers:
          type: array
          items:
            $ref: '#/components/schemas/AgentVerifier'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/AgentArtifact'
    PromoteAgentArtifactRequest:
      type: object
      additionalProperties: false
      required: [artifact_ref, state, source_ref]
      properties:
        run_id:
          type: string
          description: Optional body alias; path id is authoritative for REST.
        artifact_ref:
          type: string
          minLength: 1
          description: Existing run or step artifact ref. Absolute roots and traversal are rejected.
        artifact_kind:
          type: string
        state:
          type: string
          enum: [candidate, validated, promoted, rejected]
        source_ref:
          type: string
          minLength: 1
          description: Safe runtime source ref for the evidence decision.
        verifier_ref:
          type: string
          description: Required for validated, promoted, and rejected decisions.
        decision:
          type: string
          maxLength: 500
          description: Required for validated, promoted, and rejected decisions; raw source, prompts, stderr, secrets, provider payloads, and PII are prohibited.
    AgentRun:
      type: object
      additionalProperties: false
      required: [id, project_id, status, started_at]
      properties:
        id:
          type: string
        project_id:
          type: string
        task_id:
          type: string
        status:
          type: string
          enum: [running, completed, failed]
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        failure_category:
          type: string
        summary:
          type: string
        changed_files:
          type: array
          items:
            type: string
        verifiers:
          type: array
          items:
            $ref: '#/components/schemas/AgentVerifier'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/AgentArtifact'
        promotions:
          type: array
          items:
            $ref: '#/components/schemas/AgentPromotion'
        steps:
          type: array
          items:
            $ref: '#/components/schemas/AgentStep'
      description: Metadata only; raw prompts, completions, source dumps, raw stderr, secrets, credentials, provider payloads, absolute roots, and PII are prohibited.
    AgentStep:
      type: object
      additionalProperties: false
      required: [id, status, started_at]
      properties:
        id:
          type: string
        tool_name:
          type: string
        tool_category:
          type: string
        status:
          type: string
          enum: [running, completed, failed]
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        failure_category:
          type: string
        notes:
          type: string
        changed_files:
          type: array
          items:
            type: string
        verifiers:
          type: array
          items:
            $ref: '#/components/schemas/AgentVerifier'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/AgentArtifact'
    AgentVerifier:
      type: object
      additionalProperties: false
      required: [command]
      properties:
        command:
          type: string
          description: Verifier command metadata. Prefer executable-only command plus args; simple space-separated words in command are normalized into args.
        args:
          type: array
          items:
            type: string
        scope:
          type: string
        status:
          type: string
        exit_status:
          type: integer
    AgentArtifact:
      type: object
      additionalProperties: false
      required: [ref]
      properties:
        ref:
          type: string
        kind:
          type: string
    AgentPromotion:
      type: object
      additionalProperties: false
      required: [artifact_ref, state, source_ref, decided_at]
      properties:
        artifact_ref:
          type: string
        artifact_kind:
          type: string
        state:
          type: string
          enum: [candidate, validated, promoted, rejected]
        source_ref:
          type: string
        verifier_ref:
          type: string
        decision:
          type: string
        decided_at:
          type: string
          format: date-time
    ProjectList:
      type: object
      additionalProperties: false
      required: [projects]
      properties:
        projects:
          type: array
          items:
            $ref: '#/components/schemas/Project'
    Project:
      type: object
      additionalProperties: false
      required: [id, display_name, enabled, classification, graph_namespace, graph_storage, digest_mode, update_policy, workspace_mode, validation_status]
      properties:
        id:
          type: string
        aliases:
          type: array
          items:
            type: string
          description: Safe alternate lookup IDs such as Go module paths. Aliases are not local root paths.
        display_name:
          type: string
        description:
          type: string
        enabled:
          type: boolean
        classification:
          type: string
        graph_namespace:
          type: string
        graph_storage:
          type: string
          enum: [persistent, in_memory]
        digest_mode:
          type: string
          enum: [metadata_only, content_graph]
        update_policy:
          type: string
          enum: [manual, live]
        workspace_mode:
          type: string
          enum: [disabled, read_only, edit]
        validation_status:
          type: string
        validation_error:
          type: string
      description: Project metadata response intentionally omits local root paths, include/exclude patterns, raw source content, and file-content hashes.
    ContextHealth:
      type: object
      additionalProperties: false
      required: [project_id, status, enabled, digest_mode, update_policy, workspace_mode, graph_storage, validation_status, eligible_file_count, indexed_symbol_count, indexed_chunk_count, search_index, workspace_git_available, checked_at]
      properties:
        project_id:
          type: string
        status:
          type: string
          enum: [ready, warming_up, running, syncing, degraded, stale, empty, disabled, unavailable]
        enabled:
          type: boolean
        digest_mode:
          type: string
        update_policy:
          type: string
        workspace_mode:
          type: string
        graph_storage:
          type: string
        validation_status:
          type: string
        status_reason:
          type: string
        latest_run:
          $ref: '#/components/schemas/ContextRunSummary'
        active_run_id:
          type: string
        eligible_file_count:
          type: integer
          minimum: 0
        indexed_symbol_count:
          type: integer
          minimum: 0
        indexed_chunk_count:
          type: integer
          minimum: 0
        search_index:
          $ref: '#/components/schemas/SearchIndexHealth'
        workspace_git_available:
          type: boolean
        reason_counts:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        checked_at:
          type: string
          format: date-time
    ContextRunSummary:
      type: object
      additionalProperties: false
      properties:
        id: {type: string}
        status: {type: string}
        trigger: {type: string}
        run_kind: {type: string}
        mode: {type: string}
        files_seen: {type: integer, minimum: 0}
        files_ingested: {type: integer, minimum: 0}
        files_skipped: {type: integer, minimum: 0}
        files_unchanged: {type: integer, minimum: 0}
        chunks_stored: {type: integer, minimum: 0}
        symbols_stored: {type: integer, minimum: 0}
        error_category: {type: string}
        reason_counts:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        current_phase: {type: string}
        started_at: {type: string, format: date-time}
        finished_at: {type: string, format: date-time}
        heartbeat_at: {type: string, format: date-time}
        last_progress_at: {type: string, format: date-time}
    SearchIndexHealth:
      type: object
      additionalProperties: false
      required: [status, degraded]
      properties:
        status:
          type: string
        degraded:
          type: boolean
        reason:
          type: string
    ImpactAnalysisRequest:
      type: object
      additionalProperties: false
      properties:
        changed_paths:
          type: array
          items:
            type: string
            description: Safe project-relative path. Absolute paths and traversal are ignored.
        diff_scope:
          type: string
          enum: [working_tree, staged, head]
        max_diff_bytes:
          type: integer
          minimum: 1
          maximum: 262144
    ImpactAnalysis:
      type: object
      additionalProperties: false
      required: [project_id, workspace_diff_used]
      properties:
        project_id:
          type: string
        diff_scope:
          type: string
        changed_paths:
          type: array
          items: {type: string}
        affected_domains:
          type: array
          items:
            $ref: '#/components/schemas/ImpactDomain'
        affected_routes:
          type: array
          items: {type: string}
        affected_tools:
          type: array
          items: {type: string}
        security_flags:
          type: array
          items: {type: string}
        source_anchors:
          type: array
          items:
            $ref: '#/components/schemas/SourceAnchor'
        residual_unknowns:
          type: array
          items: {type: string}
        partial:
          type: boolean
        partial_reason:
          type: string
        workspace_diff_used:
          type: boolean
        workspace_truncated:
          type: boolean
    ContextPackBuildRequest:
      type: object
      additionalProperties: false
      properties:
        query:
          type: string
          description: Optional literal search query. Empty query returns a bounded file sample plus warning.
        path_prefix:
          type: string
          description: Safe project-relative prefix. Absolute paths and traversal are rejected.
        changed_paths:
          type: array
          items:
            type: string
            description: Safe project-relative paths for impact analysis and indexed-file lookup.
        diff_scope:
          type: string
          enum: [working_tree, staged, head]
        max_diff_bytes:
          type: integer
          minimum: 1
          maximum: 262144
        max_items:
          type: integer
          minimum: 1
          maximum: 25
        max_snippet_bytes:
          type: integer
          minimum: 1
          maximum: 4096
        include_impact:
          type: boolean
    ContextPack:
      type: object
      additionalProperties: false
      required: [project_id, limits]
      properties:
        project_id:
          type: string
        query:
          type: string
        path_prefix:
          type: string
        changed_paths:
          type: array
          items: {type: string}
        limits:
          $ref: '#/components/schemas/ContextPackLimits'
        text_hits:
          type: array
          items:
            $ref: '#/components/schemas/ProjectTextSearchResult'
          description: Capped snippets only; chunk text is intentionally empty.
        files:
          type: array
          items:
            $ref: '#/components/schemas/ProjectFile'
        symbols:
          type: array
          items:
            $ref: '#/components/schemas/ProjectSymbol'
        impact:
          $ref: '#/components/schemas/ImpactAnalysis'
        partial:
          type: boolean
        warnings:
          type: array
          items: {type: string}
        limitations:
          type: array
          items: {type: string}
    ContextPackLimits:
      type: object
      additionalProperties: false
      required: [max_items, max_snippet_bytes]
      properties:
        max_items:
          type: integer
        max_snippet_bytes:
          type: integer
    ImpactDomain:
      type: object
      additionalProperties: false
      required: [name, reason, confidence]
      properties:
        name: {type: string}
        reason: {type: string}
        confidence: {type: string}
        paths:
          type: array
          items: {type: string}
    SourceAnchor:
      type: object
      additionalProperties: false
      required: [path]
      properties:
        path: {type: string}
        kind: {type: string}
    ClaimCheckRequest:
      type: object
      additionalProperties: false
      properties:
        documents:
          type: array
          items:
            $ref: '#/components/schemas/ClaimDocument'
        selected_paths:
          type: array
          items:
            type: string
            description: "Stable docs/contracts only: README.md, docs/, api/, or .ai/skills/mivia-mcp/SKILL.md."
        known_tools:
          type: array
          items: {type: string}
        known_routes:
          type: array
          items: {type: string}
    ClaimDocument:
      type: object
      additionalProperties: false
      required: [path, text]
      properties:
        path: {type: string}
        text:
          type: string
          description: Document text supplied by caller. Response findings do not echo document content.
    ClaimCheckResult:
      type: object
      additionalProperties: false
      required: [claims]
      properties:
        project_id:
          type: string
        claims:
          type: array
          items:
            $ref: '#/components/schemas/ClaimFinding'
    ClaimFinding:
      type: object
      additionalProperties: false
      required: [path, line, kind, status, safe_message]
      properties:
        path: {type: string}
        line: {type: integer, minimum: 0}
        kind: {type: string}
        claim: {type: string}
        status:
          type: string
          enum: [verified, stale, unverified, out_of_scope]
        evidence: {type: string}
        safe_message: {type: string}
    WorkspaceGitStatus:
      type: object
      additionalProperties: false
      required: [project_id, entries, truncated]
      properties:
        project_id:
          type: string
        branch:
          type: string
        head_oid_short:
          type: string
        entries:
          type: array
          items:
            $ref: '#/components/schemas/WorkspaceGitStatusEntry'
        truncated:
          type: boolean
        next_page_token:
          type: string
    WorkspaceGitStatusEntry:
      type: object
      additionalProperties: false
      required: [relative_path, status]
      properties:
        relative_path:
          type: string
        status:
          type: string
        staged_status:
          type: string
        worktree_status:
          type: string
        renamed_from:
          type: string
    WorkspaceGitDiff:
      type: object
      additionalProperties: false
      required: [project_id, scope, files, diff_truncated]
      properties:
        project_id:
          type: string
        scope:
          type: string
          enum: [working_tree, staged, head]
        files:
          type: array
          items:
            $ref: '#/components/schemas/WorkspaceGitDiffFile'
        skipped:
          type: array
          items:
            $ref: '#/components/schemas/WorkspaceGitDiffSkip'
        diff_truncated:
          type: boolean
        next_page_token:
          type: string
    WorkspaceGitDiffFile:
      type: object
      additionalProperties: false
      required: [relative_path, status, additions, deletions, diff]
      properties:
        relative_path:
          type: string
        status:
          type: string
        additions:
          type: integer
          minimum: 0
        deletions:
          type: integer
          minimum: 0
        diff:
          type: string
    WorkspaceGitDiffSkip:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        relative_path:
          type: string
        reason:
          type: string
    WorkspaceFile:
      type: object
      additionalProperties: false
      required: [project_id, relative_path, size_bytes, modified_at, text, text_truncated, line_count]
      properties:
        file_id:
          type: string
        project_id:
          type: string
        relative_path:
          type: string
        extension:
          type: string
        size_bytes:
          type: integer
          minimum: 0
        modified_at:
          type: string
          format: date-time
        text:
          type: string
        text_truncated:
          type: boolean
        line_count:
          type: integer
          minimum: 0
        edit_token:
          type: string
          description: Opaque per-process token; not a content hash.
    WorkspaceFileEditRequest:
      type: object
      additionalProperties: false
      required: [edit_token, edits]
      properties:
        file_id:
          type: string
        relative_path:
          type: string
        edit_token:
          type: string
        dry_run:
          type: boolean
        edits:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/WorkspaceExactEdit'
    WorkspaceExactEdit:
      type: object
      additionalProperties: false
      required: [start_byte, end_byte, old_text, new_text]
      properties:
        start_byte:
          type: integer
          minimum: 0
        end_byte:
          type: integer
          minimum: 0
        old_text:
          type: string
        new_text:
          type: string
    WorkspaceFileEditResult:
      type: object
      additionalProperties: false
      required: [applied, file, text_truncated]
      properties:
        applied:
          type: boolean
        file:
          $ref: '#/components/schemas/WorkspaceFile'
        diff_preview:
          type: string
        text_truncated:
          type: boolean
        ingestion_run_id:
          type: string
        new_edit_token:
          type: string
    ProjectDigestRun:
      type: object
      additionalProperties: false
      required: [id, project_id, graph_namespace, status, files_scanned, files_stored, files_skipped, started_at, completed_at]
      properties:
        id:
          type: string
        project_id:
          type: string
        graph_namespace:
          type: string
        status:
          type: string
          enum: [completed, failed]
        files_scanned:
          type: integer
          minimum: 0
        files_stored:
          type: integer
          minimum: 0
        files_skipped:
          type: integer
          minimum: 0
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
      description: Digest run metadata only; raw source content and file-content hashes are not stored or returned.
    ProjectIngestionRun:
      type: object
      additionalProperties: false
      required: [id, project_id, trigger, mode, status, files_seen, files_ingested, files_skipped, chunks_stored, symbols_stored, started_at, finished_at]
      properties:
        id:
          type: string
        project_id:
          type: string
        trigger:
          type: string
          enum: [manual, live]
        run_kind:
          type: string
          enum: [full_scan, delta, search_index_rebuild]
        mode:
          type: string
          enum: [content_graph]
        status:
          type: string
          enum: [pending, running, completed, failed]
        files_seen:
          type: integer
          minimum: 0
        files_ingested:
          type: integer
          minimum: 0
        files_skipped:
          type: integer
          minimum: 0
        chunks_stored:
          type: integer
          minimum: 0
        symbols_stored:
          type: integer
          minimum: 0
        error_category:
          type: string
        reason_counts:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        started_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
    ProjectFileList:
      type: object
      additionalProperties: false
      required: [files]
      properties:
        files:
          type: array
          items:
            $ref: '#/components/schemas/ProjectFile'
        next_page_token:
          type: string
        index:
          $ref: '#/components/schemas/ProjectSearchIndex'
    ProjectFile:
      type: object
      additionalProperties: false
      required: [id, project_id, status, present, size_bytes, modified_at, relative_path_safe]
      properties:
        id:
          type: string
          description: Stable opaque file id; never an absolute path.
        project_id:
          type: string
        relative_path:
          type: string
          description: Root-relative path for safe eligible files only.
        extension:
          type: string
          description: Lowercase file extension, including leading dot, for safe relative paths only.
        status:
          type: string
          enum: [eligible, skipped, absent]
        present:
          type: boolean
        size_bytes:
          type: integer
          minimum: 0
        modified_at:
          type: string
          format: date-time
        skipped_reason:
          type: string
        relative_path_safe:
          type: boolean
      description: Does not include absolute roots, source-content hashes, skipped sensitive content, or matched sensitive text.
    ProjectChunkList:
      type: object
      additionalProperties: false
      required: [chunks]
      properties:
        chunks:
          type: array
          items:
            $ref: '#/components/schemas/ProjectChunk'
        next_page_token:
          type: string
    ProjectChunk:
      type: object
      additionalProperties: false
      required: [id, file_id, project_id, index, start_line, end_line, byte_start, byte_end, text, text_truncated]
      properties:
        id:
          type: string
        file_id:
          type: string
        project_id:
          type: string
        index:
          type: integer
          minimum: 0
        start_line:
          type: integer
          minimum: 1
        end_line:
          type: integer
          minimum: 1
        byte_start:
          type: integer
          minimum: 0
        byte_end:
          type: integer
          minimum: 0
        text:
          type: string
          description: Bounded eligible chunk text only; never skipped sensitive content.
        text_truncated:
          type: boolean
    ProjectFileOutline:
      type: object
      additionalProperties: false
      required: [file]
      properties:
        file:
          $ref: '#/components/schemas/ProjectFile'
        headings:
          type: array
          items:
            $ref: '#/components/schemas/ProjectHeading'
        symbols:
          type: array
          items:
            $ref: '#/components/schemas/ProjectSymbol'
        symbols_next_page_token:
          type: string
        chunks:
          type: array
          items:
            $ref: '#/components/schemas/ProjectOutlineChunk'
      description: Chunk text is omitted unless requested with include_chunk_text. Raw AST node text, absolute roots, skipped sensitive content, and matched sensitive text are never returned.
    ProjectHeading:
      type: object
      additionalProperties: false
      required: [id, file_id, project_id, level, text, parent_index, start_line, end_line]
      properties:
        id:
          type: string
        file_id:
          type: string
        project_id:
          type: string
        level:
          type: integer
          minimum: 1
        text:
          type: string
        parent_index:
          type: integer
        start_line:
          type: integer
          minimum: 1
        end_line:
          type: integer
          minimum: 1
    ProjectOutlineChunk:
      type: object
      additionalProperties: false
      required: [id, file_id, project_id, index, start_line, end_line, byte_start, byte_end]
      properties:
        id:
          type: string
        file_id:
          type: string
        project_id:
          type: string
        index:
          type: integer
          minimum: 0
        start_line:
          type: integer
          minimum: 1
        end_line:
          type: integer
          minimum: 1
        byte_start:
          type: integer
          minimum: 0
        byte_end:
          type: integer
          minimum: 0
        text:
          type: string
          description: Optional eligible chunk text, included only when requested and capped by max_chunk_bytes and project limits.
        text_truncated:
          type: boolean
    ProjectSymbolList:
      type: object
      additionalProperties: false
      required: [symbols]
      properties:
        symbols:
          type: array
          items:
            $ref: '#/components/schemas/ProjectSymbol'
        next_page_token:
          type: string
        index:
          $ref: '#/components/schemas/ProjectSearchIndex'
    ProjectTextSearchResultList:
      type: object
      additionalProperties: false
      required: [results, max_snippet_bytes]
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ProjectTextSearchResult'
        next_page_token:
          type: string
        max_snippet_bytes:
          type: integer
          minimum: 1
        index:
          $ref: '#/components/schemas/ProjectSearchIndex'
    ProjectTextSearchResult:
      type: object
      additionalProperties: false
      required: [file, chunk, line_start, line_end, byte_start, byte_end, snippet, snippet_truncated]
      properties:
        file:
          $ref: '#/components/schemas/ProjectFile'
        chunk:
          $ref: '#/components/schemas/ProjectChunk'
        line_start: {type: integer, minimum: 1}
        line_end: {type: integer, minimum: 1}
        byte_start: {type: integer, minimum: 0}
        byte_end: {type: integer, minimum: 0}
        snippet:
          type: string
          description: Capped eligible indexed source snippet only.
        snippet_truncated:
          type: boolean
    ProjectASTSearchResultList:
      type: object
      additionalProperties: false
      required: [results, query_language, query_version, result_truncated, max_snippet_bytes]
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ProjectASTSearchResult'
        next_page_token:
          type: string
        query_language:
          type: string
        query_version:
          type: string
        result_truncated:
          type: boolean
        max_snippet_bytes:
          type: integer
          minimum: 1
        coverage:
          $ref: '#/components/schemas/ProjectASTCoverage'
          description: Safe coverage-gap metadata only. It does not expose source text, paths for sensitive skips, content hashes, or parser internals.
        index:
          $ref: '#/components/schemas/ProjectSearchIndex'
    ProjectASTQueryCatalog:
      type: object
      additionalProperties: false
      required: [queries]
      properties:
        queries:
          type: array
          items:
            $ref: '#/components/schemas/ProjectASTQuery'
        coverage:
          type: array
          description: Safe per-language coverage-gap metadata only. It does not expose source text, paths for sensitive skips, content hashes, or parser internals.
          items:
            $ref: '#/components/schemas/ProjectASTCoverage'
        index:
          $ref: '#/components/schemas/ProjectSearchIndex'
    ProjectASTQuery:
      type: object
      additionalProperties: false
      required: [id, language, version, captures, extensions]
      properties:
        id:
          type: string
          description: Named query id. This is not raw Tree-sitter query syntax.
        language:
          type: string
          enum: [go, python, javascript, jsx, typescript, tsx, csharp, dart]
        version:
          type: string
        captures:
          type: array
          items: {type: string}
        extensions:
          type: array
          items: {type: string}
    ProjectASTCoverage:
      type: object
      additionalProperties: false
      required: [language, extensions, coverage_scope, eligible_files, skipped_file_too_large, coverage_status]
      properties:
        language:
          type: string
        extensions:
          type: array
          items: {type: string}
        coverage_scope:
          type: string
          enum: [file_too_large]
          description: Scope of the coverage gap counters; this does not mean complete semantic coverage for every possible skip reason.
        eligible_files:
          type: integer
          minimum: 0
        skipped_file_too_large:
          type: integer
          minimum: 0
          description: Count of oversized skipped files in this language coverage set. File paths and source are not returned.
        coverage_status:
          type: string
          enum: [complete, partial]
        coverage_partial_cause:
          type: string
          enum: [file_too_large]
    ProjectASTSearchResult:
      type: object
      additionalProperties: false
      required: [file, chunk, capture_name, line_start, line_end, byte_start, byte_end]
      properties:
        file:
          $ref: '#/components/schemas/ProjectFile'
        chunk:
          $ref: '#/components/schemas/ProjectChunk'
        capture_name:
          type: string
        capture_text:
          type: string
          description: Capped eligible capture text only.
        capture_text_truncated:
          type: boolean
        line_start: {type: integer, minimum: 1}
        line_end: {type: integer, minimum: 1}
        byte_start: {type: integer, minimum: 0}
        byte_end: {type: integer, minimum: 0}
        start_column: {type: integer, minimum: 0}
        end_column: {type: integer, minimum: 0}
        snippet:
          type: string
          description: Capped eligible indexed source snippet only.
        snippet_truncated:
          type: boolean
    ProjectSearchIndex:
      type: object
      additionalProperties: false
      properties:
        index_status:
          type: string
          enum: [completed, running, failed, pending, unknown]
        ingestion_run_id:
          type: string
        degraded:
          type: boolean
          description: True when the local search index is known to require repair.
        degraded_reason:
          type: string
          description: Sanitized reason code when the local search index is degraded.
    ProjectSymbol:
      type: object
      additionalProperties: false
      required: [id, file_id, project_id, kind, name, start_line, end_line]
      properties:
        id:
          type: string
        file_id:
          type: string
        project_id:
          type: string
        kind:
          type: string
        name:
          type: string
        package:
          type: string
        import_path:
          type: string
        receiver:
          type: string
        start_line:
          type: integer
          minimum: 1
        end_line:
          type: integer
          minimum: 1
        start_byte:
          type: integer
          minimum: 0
        end_byte:
          type: integer
          minimum: 0
        start_column:
          type: integer
          minimum: 0
        end_column:
          type: integer
          minimum: 0
    ProjectSymbolSource:
      type: object
      additionalProperties: false
      required: [symbol, text, text_truncated, max_bytes]
      properties:
        symbol:
          $ref: '#/components/schemas/ProjectSymbol'
        text:
          type: string
          description: Eligible indexed symbol source text, capped by max_source_bytes and project limits.
        text_truncated:
          type: boolean
        max_bytes:
          type: integer
    ProjectSymbolReferenceList:
      type: object
      additionalProperties: false
      required: [symbol, references]
      properties:
        symbol:
          $ref: '#/components/schemas/ProjectSymbol'
        references:
          type: array
          items:
            $ref: '#/components/schemas/ProjectSymbolReference'
        next_page_token:
          type: string
        index:
          $ref: '#/components/schemas/ProjectSearchIndex'
    ProjectSymbolReference:
      type: object
      additionalProperties: false
      required: [id, file_id, project_id, kind, name, start_line, end_line, resolution_status]
      properties:
        id: {type: string}
        file_id: {type: string}
        project_id: {type: string}
        kind: {type: string}
        name: {type: string}
        target_name: {type: string}
        target_symbol_id: {type: string}
        package: {type: string}
        receiver: {type: string}
        import_path: {type: string}
        enclosing_symbol_id: {type: string}
        enclosing_symbol_name: {type: string}
        start_line: {type: integer, minimum: 1}
        end_line: {type: integer, minimum: 1}
        start_byte: {type: integer, minimum: 0}
        end_byte: {type: integer, minimum: 0}
        start_column: {type: integer, minimum: 0}
        end_column: {type: integer, minimum: 0}
        resolution_status: {type: string}
        confidence: {type: string}
    ProjectSymbolCallEdgeList:
      type: object
      additionalProperties: false
      required: [symbol, edges]
      properties:
        symbol:
          $ref: '#/components/schemas/ProjectSymbol'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/ProjectSymbolCallEdge'
        next_page_token:
          type: string
        index:
          $ref: '#/components/schemas/ProjectSearchIndex'
    ProjectSymbolCallEdge:
      type: object
      additionalProperties: false
      required: [id, project_id, caller_symbol_id, callee_symbol_id, resolution_status]
      properties:
        id: {type: string}
        call_id: {type: string}
        file_id: {type: string}
        project_id: {type: string}
        caller_symbol_id: {type: string}
        callee_symbol_id: {type: string}
        caller_name: {type: string}
        callee_name: {type: string}
        receiver: {type: string}
        import_path: {type: string}
        start_line: {type: integer, minimum: 1}
        end_line: {type: integer, minimum: 1}
        start_byte: {type: integer, minimum: 0}
        end_byte: {type: integer, minimum: 0}
        start_column: {type: integer, minimum: 0}
        end_column: {type: integer, minimum: 0}
        resolution_status: {type: string}
        confidence: {type: string}
    ProjectSymbolCallGraph:
      type: object
      additionalProperties: false
      required: [symbol, direction, max_depth, max_nodes, nodes, edges, truncated]
      properties:
        symbol:
          $ref: '#/components/schemas/ProjectSymbol'
        direction: {type: string}
        max_depth: {type: integer}
        max_nodes: {type: integer}
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/ProjectSymbol'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/ProjectSymbolCallEdge'
        truncated:
          type: boolean
    ErrorResponse:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          type: object
          additionalProperties: false
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string
